% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_clean.R
\name{runPseudoDEG}
\alias{runPseudoDEG}
\title{Function to identify differentially expressed genes (DEGs) along pseudotime trajectories
for different cell populations or nodes in a hierarchical structure}
\usage{
runPseudoDEG(
  sample_meta,
  pb.ls,
  leaves_info,
  severity_name = "severity",
  sample_name = "sample",
  pseudotime_name = "pseudotime",
  gene_mapping = F,
  deg_parallel = F,
  deg_num_cores = 10
)
}
\arguments{
\item{sample_meta}{Data frame containing sample metadata including severity and pseudotime information}

\item{pb.ls}{List containing pseudobulk expression data, with 'all' component containing}

\item{leaves_info}{Data frame containing hierarchical structure information with columns:}

\item{severity_name}{Column name in sample_meta for severity information (default: 'severity')}

\item{sample_name}{Column name in sample_meta identifying samples (default: 'sample')}

\item{pseudotime_name}{Column name in sample_meta for pseudotime values (default: 'pseudotime')}

\item{gene_mapping}{Logical indicating whether to map genes to additional identifiers (default: FALSE)}

\item{deg_parallel}{Logical indicating whether to perform parallel processing (default: FALSE)}

\item{deg_num_cores}{Number of cores to use for parallel processing (default: 10)

Returns:
List containing two components:
\itemize{
\item tab: List of DEG analysis results for each node/population, including:
gene names, p-values, FDR, effect sizes, and significance calls
\item num_DEG: Summary data frame with number of DEGs per node in the hierarchy
}

Details:
The function performs differential expression analysis using GAM models to identify
genes that change significantly along pseudotime. For each node in the hierarchy:
\enumerate{
\item Fits GAM model to expression data using pseudotime
\item Calculates effect sizes and significance
\item Adjusts for multiple testing
\item Optionally maps genes to additional identifiers
\item Summarizes results in hierarchy context
}}
}
\description{
Parameters:
}
